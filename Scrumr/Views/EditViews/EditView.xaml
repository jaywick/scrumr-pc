<Controls:MetroWindow  x:Class="Scrumr.EditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="PropertiesView" Height="356" Width="554" WindowStartupLocation="CenterScreen"
        GlowBrush="{DynamicResource AccentColorBrush}" WindowTitleBrush="White" TitleForeground="Black" Background="White" NonActiveWindowTitleBrush="White" ShowTitleBar="False">
    
    <Grid Margin="0 30 0 0">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" >
            <StackPanel x:Name="Contents" Height="Auto" Grid.Row="0" Margin="10"/>
        </ScrollViewer>
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
            <Button Margin="5" Width="150" Click="Cancel_Click" Style="{DynamicResource GenericButton}" IsCancel="True">Discard</Button>
            <Button Margin="5" Width="200" Click="Save_Click" Style="{DynamicResource AccentedButton}" IsDefault="True">Accept</Button>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
