<Window x:Class="Scrumr.PropertiesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PropertiesView" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="Contents" Grid.Row="0" Margin="10"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
            <Button Width="100" Click="Save_Click">Save</Button>
            <Button Width="100" Click="Cancel_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
