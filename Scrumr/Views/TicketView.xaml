<UserControl x:Class="Scrumr.TicketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200">
    <Grid Margin="8,4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border x:Name="borderId" Background="#FFFFE4E4" CornerRadius="3">
            <TextBlock Grid.Column="0" x:Name="labelId" Text="" VerticalAlignment="Center" Padding="5,3"/>
        </Border>
        <TextBlock Grid.Column="2" x:Name="labelName" Text="" VerticalAlignment="Center"/>
    </Grid>
    
</UserControl>
