<UserControl x:Class="Scrumr.Client.FeatureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Label        Grid.Row="0" x:Name="labelProject" FontSize="36" Content="Project" MouseDown="labelProject_MouseDown"/>
        <ScrollViewer Grid.Row="1" PanningMode="VerticalOnly">
            <WrapPanel x:Name="RootItems" Margin="20"/>
        </ScrollViewer>
    </Grid>
</UserControl>
